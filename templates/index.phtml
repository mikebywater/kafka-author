<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/paper.min.css">
    <script src="/js/jquery-3.3.1.min.js"></script>
    <title>Document</title>
</head>
<body>
<div class="paper container">
    <div class="row">
        <div class="col-12 col">
            <img src="/img/logo.webp" class="no-border float-left logo">
            <h3> Kafka Author</h3>
            <p>Produces a kafka event based on the payload you author.</p>
        </div>
    </div>
    <div class="row" id="form">
        <div class="col-6 col">
            <div class="form-group">
                <label for="broker">Broker IP / Address</label>
                <input type="text" placeholder="eg. 192.168.10.10" id="broker" name="broker">
            </div>
            <div class="form-group">
                <label for="topic">Topic Name</label>
                <input type="text" placeholder="eg. newTopic" id="topic" name="topic">
            </div>
        </div>
        <div class="col-6 col">
            <div class="form-group">
                <label>Event Payload</label>
                <textarea id="payload" name="payload" placeholder='eg. {"name":"mike bywater"}'></textarea>
            </div>
            <button id="submit" class="btn-success">Submit</button>
        </div>
    </div>

</div>
</body>
<style>
    .logo{
        height:200px;
    }
</style>
<script>
    $(document).ready(function() {
        $(window).on("unload", function(e) {
            saveSettings();
        });
        loadSettings();
        $('#submit').click(function(){
            $.post( "/", $( "#form :input" ).serialize() )
                .done(function( data ) {
             //       alert( "Data Loaded: " + data );
                });
        })
    });

    function loadSettings() {
        $('#topic').val(localStorage.topic);
        $('#broker').val(localStorage.broker);
        $('#payload').val(localStorage.payload);
    }

    function saveSettings() {
        localStorage.broker = $('#broker').val();
        localStorage.topic = $('#topic').val();
        localStorage.payload = $('#payload').val();
    }
</script>
</html>

